# GitLab CI/CD for Empire: Wargame of the Century
# Windows build using LDC2 and DUB

stages:
  - build

variables:
  LDC_VERSION: "1.38.0"

build-windows:
  stage: build
  tags:
    - windows
    - shell
  variables:
    ErrorActionPreference: stop
  before_script:
    # Install Chocolatey if not present
    - |
      if (!(Get-Command choco -ErrorAction SilentlyContinue)) {
        Set-ExecutionPolicy Bypass -Scope Process -Force
        [System.Net.ServicePointManager]::SecurityProtocol = [System.Net.ServicePointManager]::SecurityProtocol -bor 3072
        iex ((New-Object System.Net.WebClient).DownloadString('https://community.chocolatey.org/install.ps1'))
      }
    # Install LDC2 D compiler
    - choco install ldc -y --version $LDC_VERSION
    # Install Windows SDK for rc.exe resource compiler
    - choco install windows-sdk-10-version-2004-windbg -y
    - Import-Module "$env:ChocolateyInstall\helpers\chocolateyProfile.psm1"
    - refreshenv
    # Find and add Windows SDK rc.exe to PATH
    - |
      $sdkPath = Get-ChildItem "C:\Program Files (x86)\Windows Kits\10\bin\*\x64\rc.exe" -ErrorAction SilentlyContinue | Select-Object -Last 1
      if ($sdkPath) {
        $env:PATH = "$($sdkPath.DirectoryName);$env:PATH"
        Write-Host "Added Windows SDK to PATH: $($sdkPath.DirectoryName)"
      } else {
        Write-Error "rc.exe not found in Windows SDK"
        exit 1
      }
    - ldc2 --version
    - dub --version
    - rc /?
  script:
    # Compile Windows resources
    - rc /nologo /fo empire.res empire.rc
    # Build release version
    - dub build --build=release --compiler=ldc2
  artifacts:
    name: "empire-windows-$CI_COMMIT_SHORT_SHA"
    paths:
      - empire.exe
      - "*.bmp"
      - "*.wav"
      - help.txt
    expire_in: 30 days
  only:
    - master
    - main
    - merge_requests
    - tags
